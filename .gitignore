node_modules/
dist/

# Circuit artifacts — generated by scripts/setup.sh, not committed to git
circuits/*.wasm
circuits/*.zkey
circuits/*.r1cs
circuits/*.sym
circuits/pot*.ptau
circuits/*_js/
# verification_key.json is small and safe to commit once trusted setup is finalised
